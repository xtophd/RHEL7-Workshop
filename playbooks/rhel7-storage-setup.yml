---
- hosts: node1.example.com
  tasks:

    - name: "SETUP STORAGE: install packages"
      yum: name=system-storage-manager state=installed

    - name: "SETUP STORAGE: system service settings"
      service: name="{{ item.svc }}" state=started enabled=yes
      with_items:
        - {svc: pmcd}
        - {svc: pmlogger}
        - {svc: numad}

    - name: "SETUP STORAGE: cheat script installs"
      template:
        src: "{{ item.src }}"
        dest: "{{ item.dest }}"
        owner: root
        group: root
        mode: 0755
      with_items:
        - {src: 'cheat-checkhost.j2', dest: '/usr/local/bin/cheat-storage-checkhost.sh'}
