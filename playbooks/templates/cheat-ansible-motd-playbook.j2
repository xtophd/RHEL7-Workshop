#!/bin/bash

FILENAME="/root/roles/motd/defaults/use-motd-role.yml"

echo "# Creating file ${FILENAME}'"

cat > ${FILENAME} << EOF
---
- name: Use MOTD role playbook
  hosts: node2.example.com
  user: root
  become: false
  roles:
  - motd
EOF

echo "# Here are the contents of ${FILENAME}"

cat ${FILENAME}
