#!/bin/bash

FILENAME="/root/roles/motd/tasks/main.yml"

echo "# Creating file ${FILENAME}'"

cat > ${FILENAME} << EOF
---
# Tasks file for MOTD

- name: Deliver MOTD file
  template: 
    src: templates/motd.j2
    dest: /etc/motd
    owner: root
    group: root
    mode: 0444
EOF

echo "# Here are the contents of ${FILENAME}"

cat ${FILENAME}
